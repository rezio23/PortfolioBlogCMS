# Project Notes

- Settings are split into `base.py`, `development.py`, and `production.py`.
- MySQL connection details are loaded from `.env`.
- Templates are organized by app for clarity and future scaling.
